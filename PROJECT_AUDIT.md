# 原项目审计与 iDeal 修复结论

审计范围：`ai_cache.json`、`ai_filter.py`、`common.py`、`feeds.json`、`ios_digest.py`、`ios_source_probe.py`、两份探针报告、`ios_watchlist.py`、`watchlist_ids.json`。

## 总结

旧项目“能运行”，但主 RSS 实际没有进入有效筛选链路，摘要结果主要由 Apple 付费榜单补位；同时首次发现降价、跨脚本状态、目标价币种和 IAP 区分均有结构性缺陷。继续在原脚本上叠补丁会使状态更难推理，因此 iDeal 采用独立项目和新数据库。

## 问题与处理

| 级别 | 原问题 | 影响 | iDeal 处理 |
|---|---|---|---|
| 致命 | 只识别 `/id123` 或 `?id=`，不识别上游 `/us/6749824842` | 三个主 RSS 的 `app_id_count=0`，随后被规则全部丢弃 | 增加上游详情路径、Apple URL、查询参数和转义 URL 提取测试 |
| 致命 | 只有本地上一条价格更高才算降价 | 首次运行遇到真实优惠会永久错过 | B 级证据允许“新鲜旧/新价声明 + Apple 现价一致”首次推送 |
| 高 | Digest 与 Watchlist 都先写历史再读上一价 | 两个任务互相吞掉变化 | `price_state` 保存上一不同价格和价格周期；消费者用不同事件键 |
| 高 | Watchlist 的任意降价先于目标价判断 | “达到目标价”分支通常不可达 | 调整为限免、目标价、任意降价 |
| 高 | 一个目标价同时用于 USD/CNY/TRY | 比较没有经济意义 | 改为 `target_prices` 按区服/币种设置 |
| 高 | 通知发送前先记录 alert | 通知失败后永不重试 | 只有发送成功才记录；dry-run 不写 alert |
| 高 | App 本体与 IAP 文案混在一起 | 可能把订阅/IAP 优惠当 App 降价 | 只解析本体价格行；IAP 另存且默认不推 |
| 中 | 对每个候选、每个区服逐个请求 Apple | 请求量大、容易触发速率限制 | 按区服并发、每批最多 100 个 App ID |
| 中 | 先去重 App 再处理 | 丢掉多源证据与不同区服 | Claim 表保留每个来源、时间、价格和区服 |
| 中 | AI 在官方核价前决定候选 | 会漏掉真实优惠，也可能受错误价格误导 | 先完成官方核价，再让 AI 严格精选优质 App；失败默认不推 |
| 中 | 用英文关键词判断工具类别 | 中文/土耳其语区分类失真 | 使用 Apple `primaryGenreId` 实用分类白名单；可配置为全品类 |
| 中 | 探针只看 HTTP/解析/非空 | 欢迎页、统计页也会被判“健康” | 增加真实优惠条数、ID 率、时效、价格声明率、Apple 抽样匹配 |
| 中 | 固定 GitHub Raw 单点 | 国内网络常出现长时间超时 | Fastly CDN、jsDelivr、GitHub Raw 三级回退 |
| 低 | 多处中文呈现乱码 | 配置和通知难维护 | 全项目 UTF-8；CSV 使用 UTF-8 BOM |
| 低 | 数据表长期增长 | 长期青龙运行占用不断增加 | 自动清理 Claim/观测/健康/Alert，状态表永久保留当前状态 |

## 2026-07-29 实测

- 开发阶段自动测试：16 项通过；测试目录不参与青龙运行，交付时已移除。
- Digest 真实 dry-run：8 个启用源无抓取失败，313 条候选完成 Apple 官方核价；AI 只接收最终真实优惠候选。
- 首次运行成功识别上游明确声明且官方价格匹配的本体优惠；过期声明因官方现价不匹配被拒绝。
- Watchlist：10 个 App、26 个 App×区服组合全部有效，批量核价完成。
- 探针：Apple 榜单与 appstore-discounts 可用；Freshapps 当时 RSS 合法为空，因此标记为 degraded，而不是误判为“有优惠”。

## 推荐运行策略

1. Digest 每 3 小时运行一次，Watchlist 错开 20 分钟。
2. Probe 每天运行一次；连续空源、低匹配率或高延迟的第三方源保持关闭。
3. 默认可信度设为 B，AI 入选数量不设上限，但必须达到 8 分优秀门槛；对极端准确率要求可把可信度改为 A。
4. HK/TW/MO/PT 只在确实需要时开启，避免无意义扩展请求量。
5. 先运行 2～3 天建立榜单和 Watchlist 基线，再评估提醒覆盖率。

## 青龙运行可观测性修复

- 恢复 `GEMINI_API_KEY`，通过 Google 官方 OpenAI 兼容端点调用；默认模型为 `gemini-3.5-flash`。
- AI 自动模式固定为 Qwen → DeepSeek → Gemini → Custom，只调用第一个成功的服务；支持用 `IDEAL_AI_PROVIDER` 或 `IDEAL_AI_PROVIDER_ORDER` 覆盖。
- 新增真实环境自检，明确显示青龙当前任务进程能看到哪些 API Key，但从不输出 Key 内容。
- 青龙定时任务改为直接以 `.py` 路径开头；避免 `python3 脚本路径` 被青龙识别成普通命令并清除面板变量。项目与原脚本一致，只从当前 Python 进程的 `os.environ` 读取变量，不依赖青龙内部生成文件。
- 删除职责重复且曾写入错误任务命令的 `ql_install.sh`；`self_check.py` 只作为部署后临时验收工具。
- 修复青龙 `notify.py` 内部吞掉渠道异常时被误判为成功的问题：检查返回值和输出中的失败信息；失败不写 `alerts`，下次自动重试。
- 新增 `clear_state.py`，可分别清理 AI 缓存或 Digest/Watchlist 已提醒记录，不碰价格历史和用户配置。
- Digest 与 Watchlist 现在记录 Apple 逻辑批次、含重试的实际网络次数、批次 ID 数和耗时；Digest 另外记录 AI 外部请求次数和全部阶段耗时，能直接核对“核验了多少”和“实际请求了几次”。
- Digest、Watchlist、Probe 日志改为中文分阶段报告，给出来源、Apple 核验、过滤、AI、通知的数量和失败原因，不再输出难读的 Python 字典。
